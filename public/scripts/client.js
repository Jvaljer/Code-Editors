let currentOpenedFile = null;
let OpenedFiles = [];

const consoleDiv = document.getElementById("console");

function displayInConsole(msg, typ)
{
    consoleDiv.textContent = "<"+typ+"> - "+msg;
}

async function createTreeView(handle)
{
    //todo
}

function runJava(files)
{
    //todo
}